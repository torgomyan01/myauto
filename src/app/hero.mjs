import { heroui } from '@heroui/theme';

export default heroui({
  themes: {
    light: {
      colors: {
        default: {
          DEFAULT: '#f8f2ea',
          foreground: '#000',
        },
        secondary: {
          DEFAULT: '#1E4A2F',
          foreground: '#F8F2EA',
        },
      },
    },
  },
});
